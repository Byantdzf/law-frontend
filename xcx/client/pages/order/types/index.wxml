<!-- pages/order/index.wxml -->
<app-page id="app-page">
  <view class="flex-column container page-order-types">
    <view class="order-type" wx:for="{{ types }}" wx:key="{{ index }}">
      <text>{{ item.name }}</text>
    </view>
  </view>
</app-page>